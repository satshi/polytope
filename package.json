{
  "name": "polytope",
  "version": "1.1.0",
  "description": "Interactive viewer for convex uniform 4D polytopes",
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "tsc --outDir .test-dist --sourceMap false && node --test",
    "check": "pnpm run build && pnpm test"
  },
  "author": "",
  "license": "0BSD",
  "devDependencies": {
    "@types/three": "0.185.1",
    "typescript": "5.9.3",
    "vite": "8.1.5"
  },
  "dependencies": {
    "three": "0.185.1"
  }
}
