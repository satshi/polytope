{
  "name": "polytope",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run clean && tsc && npm run copy-assets",
    "watch": "tsc -w",
    "clean": "rm -rf dist",
    "copy-assets": "cp src/index.html dist/ && cp src/style.css dist/ && cp -r src/data dist/"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "npm": "^11.7.0",
    "three": "^0.182.0"
  }
}